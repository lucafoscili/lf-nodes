"""Locally owned Workflow Runner definitions.

Modules in this package are discovered by ``workflows.iter_workflow_modules`` so
they can extend the runner without growing its fixed packaged-workflow list.
"""
